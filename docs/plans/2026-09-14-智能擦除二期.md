# 智能擦除二期：MI-GAN 双模型 + 设置页信息架构重设计

- 需求：①增加 MI-GAN 快速模型（用户确认）；②设置页"抠图服务"tab 混入擦除模型太乱，重新设计
- 选型实测（本机 RTX 4060 / 16 逻辑核，真实水印图）：
  - `andraniksargsyan/migan/migan_pipeline_v2.onnx`（27MB）：uint8 图+遮罩进、uint8 出、动态尺寸、预处理内置图内；**CPU 1.58s**，与 LaMa 结果平均差 0.4/255；DML 兼容（2.2s，不崩但无优势）
  - LaMa（208MB）：CPU 5s（会话缓存后），质量基准
  - 弃用 lxfater/inpaint-web 的 migan.onnx：4ch 单输入约定不明，全部组合输出异常
- 设置页新 IA（按功能域分 tab）：`生图服务 | 智能抠图 | 智能擦除 | 关于`；擦除模型从抠图 tab 拆出独立 tab
- 模型约定：LaMa 输入 float [0,1] + float mask @512；MI-GAN 输入 uint8 + uint8 mask（255=擦除）@512；两者输入名恰好都是 image/mask

## 任务

- [x] T1 inpaint.rs 双模型注册表：InpaintModel 加 io 枚举（Float01 / Uint8），run_inpaint 按 io 分支构造/解析；模型操作按 id 参数化
- [x] T2 后端命令：list（含 current，读 config inpaint_model）/ import(id, path) / delete(id) / open(id) / inpaint_region(req.modelId)
- [x] T3 client.ts 同步签名；EditView 擦除抽屉加模型选择（已下载的模型二选一，存 config）
- [x] T4 SettingsView：新增「智能擦除」tab（模型卡片 ×2：下载/导入/位置/删除 + 默认模型选择），从「抠图服务」tab 移除擦除区块
- [x] T5 验证：cargo test 双模型端到端（lama 用例已有；migan 新用例）+ pnpm build + cargo check 全过
